---
phase: 03-capture-mandatory-review-ui
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/lib/types.ts
  - web/lib/api.ts
  - web/app/actions.ts
  - web/lib/useImportPolling.ts
  - web/components/GroundingBadge.tsx
autonomous: true
requirements: [CAP-01, REV-02, REV-04, REV-03]

must_haves:
  truths:
    - "The frontend can call POST /import, GET /import/:jobId, PATCH /import/:jobId/recipe, and GET /import/mine through server-action wrappers that attach the Clerk bearer token (CAP-01, REV-04)"
    - "useImportPolling polls GET /import/:jobId on a setTimeout loop, stops on ready_for_review/failed, and surfaces an explicit timeout state instead of going silent (CAP-01, Pitfall 2)"
    - "GroundingBadge renders a 'Confira isto' pill for inferred/ambiguous and nothing for grounded (REV-02)"
  artifacts:
    - "web/lib/types.ts (ImportJob, ImportJobStatus, ImportFailureReason, RecipeGrounding, GroundingLevel, ImportRecipeEditPatch, ImportedRecipeListItem — mirroring the backend contracts)"
    - "web/lib/api.ts (startImport, getImportJob, confirmImportRecipe, listMyImports)"
    - "web/app/actions.ts (startImportAction, getImportJobAction, confirmImportRecipeAction, listMyImportsAction)"
    - "web/lib/useImportPolling.ts"
    - "web/components/GroundingBadge.tsx"
  key_links:
    - "web/app/actions.ts wrappers → web/lib/api.ts (server-only, authHeaders) → /api/v1/import* — client components never fetch the backend directly"
    - "useImportPolling → getImportJobAction (poll loop calls the server action, never fetches directly)"
  prohibitions:
    - "Client components MUST NEVER call web/lib/api.ts or the backend directly — every call goes through a use-server action wrapper (the existing pattern)"
    - "useImportPolling MUST NOT use setInterval — use recursive setTimeout (schedule next poll only after the previous resolves) to avoid overlapping requests"
    - "useImportPolling MUST NOT go silent on timeout like useLazyThumbnail's stopPolling — it MUST set an explicit timed-out state the component can render (Pitfall 2)"
    - "GroundingBadge MUST NOT render a green/checkmark badge for grounded — grounded = absence of a badge (2-state system, not 3-color traffic-lighting per UI-SPEC)"
    - "No new frontend dependency (no SWR/react-query/zod) — polling/clipboard/forms use only installed React APIs (RESEARCH §Standard Stack)"
---

<objective>
Build the shared frontend foundation the four Phase 3 screens all import: the mirrored TypeScript types for the import contracts, the four `web/lib/api.ts` fetch wrappers, their `web/app/actions.ts` server-action pairs, the `useImportPolling` hook, and the `GroundingBadge` primitive. Landing these once (before the screens) keeps the screen plans small and lets `03-04`/`03-05` run in parallel without racing on these shared files.

Purpose: Every screen depends on these; extracting them into one Wave-2 plan is the interface-first ordering that unblocks parallel screen work.

Output: `web/lib/types.ts` additions, `web/lib/api.ts` + `web/app/actions.ts` wrappers, `web/lib/useImportPolling.ts`, `web/components/GroundingBadge.tsx`.

Depends on 03-01 because the api.ts wrappers and the `ImportRecipeEditPatch` type mirror the exact request/response shapes of the new backend routes (`PATCH /import/:jobId/recipe`, `GET /import/mine`) that 03-01 defines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-mandatory-review-ui/03-RESEARCH.md
@.planning/phases/03-capture-mandatory-review-ui/03-PATTERNS.md
@.planning/phases/03-capture-mandatory-review-ui/03-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

- `web/lib/types.ts` — `ImportJob`, `ImportJobStatus`, `ImportFailureReason`, `GroundingLevel`, `RecipeGrounding`, `ImportRecipeEditPatch`, `ImportedRecipeListItem`
- `web/lib/api.ts` — `startImport(url)`, `getImportJob(jobId)`, `confirmImportRecipe(jobId, patch)`, `listMyImports()`
- `web/app/actions.ts` — `startImportAction`, `getImportJobAction`, `confirmImportRecipeAction`, `listMyImportsAction`
- `web/lib/useImportPolling.ts` — setTimeout-recursion polling hook with explicit timeout state
- `web/components/GroundingBadge.tsx` — grounded/inferred/ambiguous badge primitive

<tasks>

<task type="auto">
  <name>Task 1: Mirror import contracts in web/lib/types.ts + add api.ts/actions.ts wrappers</name>
  <files>web/lib/types.ts, web/lib/api.ts, web/app/actions.ts</files>
  <read_first>
    - web/lib/types.ts (head — the manual "mirror the backend contracts" convention; note RecipeSource here does NOT yet include "imported" — add it)
    - web/lib/api.ts (searchRecipes/getRecipe/triggerThumbnail — the fetch + authHeaders() pattern; API_BASE default http://localhost:3000, routes under /api/v1/*)
    - web/app/actions.ts (adaptRecipeAction = error-normalized mutation shape; triggerThumbnailAction/getThumbnailUrlAction = thin pass-through)
    - src/modules/import/import-job.types.ts (ImportJob, ImportJobStatus, ImportFailureReason — the exact source-of-truth shapes to mirror)
    - src/modules/recipes/recipe.types.ts (RecipeGrounding, GroundingLevel, RecipeSearchHit — mirror for ImportedRecipeListItem)
    - .planning/phases/03-capture-mandatory-review-ui/03-PATTERNS.md (web/lib/api.ts additions + web/app/actions.ts additions sections — the exact wrapper shapes)
  </read_first>
  <action>
    In web/lib/types.ts add `ImportJobStatus`, `ImportFailureReason`, `GroundingLevel`, `RecipeGrounding`, and an `ImportJob` interface mirroring src/modules/import/import-job.types.ts (fields the UI reads: _id/status/failureReason/errorMessage/recipeId/reviewRequired/confidenceScore/platform/sourceMeta). Add `ImportRecipeEditPatch` matching 03-01's ImportRecipeEditSchema (title, intro, ingredients[].{name,quantity?,unit?}, steps[].{text}) and `ImportedRecipeListItem` mirroring the RecipeSearchHit fields listMyImportedRecipes returns (plus reviewRequired/confirmedAt so ImportsList can show status). Add "imported" to the frontend `RecipeSource` union. In web/lib/api.ts add `startImport(url)` (POST /api/v1/import, cache:no-store, authHeaders), `getImportJob(jobId)` (GET /api/v1/import/:jobId), `confirmImportRecipe(jobId, patch)` (PATCH /api/v1/import/:jobId/recipe), `listMyImports()` (GET /api/v1/import/mine) — copy the exact fetch/error-throw shape from 03-PATTERNS.md's api.ts additions block. In web/app/actions.ts add the four wrappers: `startImportAction` and `confirmImportRecipeAction` return the `{ ok, ... } | { ok:false, error }` normalized shape (like adaptRecipeAction, error copy in pt-BR); `getImportJobAction` and `listMyImportsAction` are thin pass-throughs (like triggerThumbnailAction). All error strings in Brazilian Portuguese.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>types.ts mirrors the import contracts (incl. "imported" source); api.ts has the four authenticated wrappers; actions.ts has the four server-action pairs with pt-BR error copy; `cd web && npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: useImportPolling hook (setTimeout-recursion, explicit timeout state)</name>
  <files>web/lib/useImportPolling.ts</files>
  <read_first>
    - web/lib/useLazyThumbnail.ts (full file — the setTimeout-recursion + cleanup-on-unmount + timeout shape to copy; note where it goes silent on timeout — this hook must NOT)
    - web/app/actions.ts (getImportJobAction — the hook calls this, never fetches directly)
    - web/lib/types.ts (ImportJob, ImportJobStatus — the hook's state type)
    - .planning/phases/03-capture-mandatory-review-ui/03-PATTERNS.md (useImportPolling section — POLL_INTERVAL_MS 2000, POLL_TIMEOUT_MS 10*60000, the exact deviation: surface timeout state, don't go silent)
    - .planning/phases/03-capture-mandatory-review-ui/03-RESEARCH.md §Common Pitfalls (Pitfall 2)
  </read_first>
  <action>
    Create `web/lib/useImportPolling.ts` ("use client"). Export `useImportPolling(jobId, initialJob)` that mirrors useLazyThumbnail's schedule/cleanup/timeout shape: a recursive setTimeout loop (POLL_INTERVAL_MS = 2000) calling `getImportJobAction(jobId)`, updating state, and re-scheduling only while status is not terminal (`ready_for_review` or `failed`). On a transient fetch error, keep polling (re-schedule, don't kill the loop). After POLL_TIMEOUT_MS (10*60000) elapses without a terminal status, set an explicit timed-out flag on the returned state (e.g. a `timedOut: boolean` alongside the job) so the progress component can render the "Isso está demorando mais que o esperado" UI — do NOT just stopPolling() silently as useLazyThumbnail does (Pitfall 2). Clear the timer on unmount and on status change. Return `{ job, timedOut }` (or the job augmented with the flag) — do NOT use setInterval.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>useImportPolling polls via getImportJobAction on a recursive setTimeout, stops on terminal status, keeps trying on transient errors, and exposes an explicit timedOut state (not silent) after POLL_TIMEOUT_MS; cleans up on unmount; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: GroundingBadge primitive (grounded = no badge; inferred/ambiguous = warn pill)</name>
  <files>web/components/GroundingBadge.tsx</files>
  <read_first>
    - web/components/NutritionBadge.tsx (full file — the rounded-full px-2 py-0.5 text-[10px] font-semibold pill idiom to copy)
    - web/lib/types.ts (GroundingLevel — the prop type)
    - .planning/phases/03-capture-mandatory-review-ui/03-UI-SPEC.md §Grounding Badge Palette (--t-warn-bg / --t-warn-fg tokens, exact labels) + §Copywriting Contract (badge copy)
    - .planning/phases/03-capture-mandatory-review-ui/03-PATTERNS.md (GroundingBadge section — the exact snippet)
  </read_first>
  <action>
    Create `web/components/GroundingBadge.tsx` exporting `GroundingBadge({ level }: { level: GroundingLevel })`. Return `null` when level is "grounded" (neutral = no badge — do NOT add a green/checkmark badge, UI-SPEC 2-state system). For "inferred" render label "Confira isto — inferido"; for "ambiguous" render "Confira isto — impreciso". Use the NutritionBadge pill classes (`inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-semibold`) with the warn palette from UI-SPEC: `background: var(--t-warn-bg)`, `color: var(--t-warn-fg)`, `border: 1px solid color-mix(in srgb, var(--t-warn-fg) 25%, transparent)`. Copy is pt-BR, exactly matching the UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>GroundingBadge renders the warn pill with the exact UI-SPEC labels for inferred/ambiguous and returns null for grounded; uses --t-warn-* tokens only (no new color); typecheck passes.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` clean after every task.
- Manual (dev server) is deferred to the screen plans (03-04/03-05) that consume these — this plan produces no user-visible screen on its own; correctness here is type-level + shape-conformance to 03-01's routes.
</verification>

<success_criteria>
- CAP-01/REV-04: the four import endpoints are reachable from the frontend via authenticated server actions.
- CAP-01 (Pitfall 2): the polling hook surfaces an explicit timeout state, never an indefinite silent spinner.
- REV-02: GroundingBadge distinguishes inferred/ambiguous from grounded per the UI-SPEC 2-state contract.
- Zero new frontend dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-capture-mandatory-review-ui/03-03-SUMMARY.md` when done
</output>
