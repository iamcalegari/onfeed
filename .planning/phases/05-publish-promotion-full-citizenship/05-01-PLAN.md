---
phase: 05-publish-promotion-full-citizenship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/recipes/recipe.types.ts
  - src/modules/recipes/recipe.model.ts
  - src/modules/recipes/recipe.model.test.ts
  - src/config/env.ts
autonomous: false
requirements: [SOC-02]
user_setup:
  - service: mongodb-atlas
    why: "npm run setup:db (collMod) must sync the live Atlas $jsonSchema validator + new unique+sparse shareSlug index before any code reads/writes shareSlug; the harness blocks .env/prod-Atlas creds so this is a documented USER GATE"
    env_vars:
      - name: MONGODB_URI
        source: ".env (already provisioned in prior phases)"
      - name: MONGODB_USERNAME
        source: ".env"
      - name: MONGODB_PASSWORD
        source: ".env"
      - name: MONGODB_DB_NAME
        source: ".env"
    dashboard_config:
      - task: "Run npm run setup:db against live Atlas to apply the shareSlug validator + share_slug_lookup unique+sparse index (collMod)"
        location: "Local shell with .env present — documented in this plan's SUMMARY as a USER GATE"
must_haves:
  truths:
    - "Recipe.shareSlug is an optional string field declared identically in the TypeScript type (recipe.types.ts) AND the BSON $jsonSchema validator (recipe.model.ts) — mongoat two-source-of-truth kept in sync in one commit"
    - "A unique + sparse index named share_slug_lookup exists on Recipe.shareSlug (unique so two recipes can never collide on a token; sparse so pre-Phase-5 recipes without shareSlug are not forced to carry a null)"
    - "env.import.promoteConfidence exists as a Number from IMPORT_PROMOTE_CONFIDENCE (default above the reviewRequired threshold of 0.6, i.e. 0.7) — a SEPARATE gate from REVIEW_SCORE_THRESHOLD"
    - "npm run setup:db has been run so the live Atlas validator + share_slug_lookup index accept shareSlug writes before any Plan 03 confirm-time write"
  artifacts:
    - src/modules/recipes/recipe.types.ts
    - src/modules/recipes/recipe.model.ts
    - src/config/env.ts
  key_links:
    - "recipe.types.ts shareSlug type <-> recipe.model.ts BSON validator MUST match exactly, or a confirm-time shareSlug write fails with DocumentValidationFailure"
    - "env.import.promoteConfidence feeds Plan 02's maybePromote confidence gate (D-06)"
    - "share_slug_lookup unique+sparse index serves Plan 03's getRecipeByShareSlug lookup and guarantees token uniqueness"
  prohibitions:
    - "The shareSlug type change NEVER lands without the matching BSON validator change in the SAME commit (mongoat two-source gotcha — Phases 2/3 UAT failed on this exact class of divergence)"
    - "shareSlug is NEVER added to the BSON required array — it stays optional (only present once confirmedAt is set, per D-04)"
    - "The share_slug_lookup index is unique AND sparse — a non-sparse unique index would reject every pre-Phase-5 recipe (they all lack shareSlug, and a plain unique index treats missing as a single null collision)"
    - "No confirm-time shareSlug write (Plan 03) happens before setup:db syncs the live validator + index — that write would fail with MongoServerError document validation"
---

<objective>
Lay the schema + config foundation for the shareable-link and public-promotion machinery: add the optional `shareSlug` field to the `Recipe` TypeScript type AND the BSON `$jsonSchema` validator (mongoat's two independent sources of truth), add a unique + sparse `share_slug_lookup` index, add the `env.import.promoteConfidence` threshold that gates public promotion, and run `npm run setup:db` to sync the live Atlas validator + index before any code reads or writes `shareSlug`.

Purpose: SOC-02 requires a secret, unguessable per-recipe token (`shareSlug`, D-03) whose uniqueness the DB must guarantee (unique index), and D-06 requires a dedicated public-promotion confidence bar (`promoteConfidence`) SEPARATE from the `reviewRequired` threshold. The mongoat two-source-of-truth gotcha (in the user's own memory, and the cause of Phase 2/3 UAT failures) makes the type↔validator sync a `[BLOCKING]` schema-sync task that must complete — including the live-Atlas `collMod` — before any Plan 03 confirm-time `shareSlug` write.
Output: `shareSlug` type + validator + unique/sparse index, `env.import.promoteConfidence`, a model-shape test, and a synced live Atlas validator + index (documented USER GATE in the SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-publish-promotion-full-citizenship/05-CONTEXT.md
@.planning/phases/05-publish-promotion-full-citizenship/05-PATTERNS.md

# Analogs / files being modified (read before implementing)
@src/modules/recipes/recipe.types.ts
@src/modules/recipes/recipe.model.ts
@src/config/env.ts
@src/infra/database/setup.ts
# Reference: the Phase-4 [BLOCKING] setup:db plan this mirrors
@.planning/phases/04-cost-quota-gating-dedup/04-02-PLAN.md
</context>

<artifacts_this_phase_produces>
- (this plan) `Recipe.shareSlug?: string` field (type + BSON), `share_slug_lookup` unique+sparse index, `env.import.promoteConfidence`
- (Plan 02) `promoteImportToPublic(recipeId)` + widened `maybePromote` (owner self-like excluded, confidence+confirmedAt gate)
- (Plan 03) `shareSlug` generation at confirm (`confirmImportedRecipe`), `getRecipeByShareSlug(token)`, `GET /recipes/share/:token` public route
- (Plan 04) `hybridSearch` widening for `source:"imported"` (owner-private + public-promoted) + "🎬 sua importação" badges
- (Plan 05) public Next page `/r/[token]`, middleware public-route allowlist, LikeButton sign-in redirect, ShareButton `/r/[slug]` wiring
- (Plan 06) verify citizenship reuse (adapt/shopping list/cook mode) + private-import search-isolation regression
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 [BLOCKING]: Add shareSlug type + BSON validator + unique/sparse index, add env.promoteConfidence, then setup:db</name>
  <read_first>
    - src/modules/recipes/recipe.types.ts (the optional-field + pt-BR comment convention around importJobId/confirmedAt at lines ~111-128 — shareSlug follows the same style)
    - src/modules/recipes/recipe.model.ts (the importJobId BSON prop at ~line 118, the indexes array at ~167-176, and specifically the external_id_unique index at ~line 170 as the unique+sparse analog)
    - src/config/env.ts (the import: {...} block at ~126-176 for placement, and variants.promoteThreshold at ~78-85 for the Number(optional(...)) threshold shape)
    - src/infra/database/setup.ts (what npm run setup:db runs: connectDatabase + setupCollections collMod + setupSearchIndexes)
  </read_first>
  <files>src/modules/recipes/recipe.types.ts, src/modules/recipes/recipe.model.ts, src/config/env.ts</files>
  <behavior>
    - A Recipe object with shareSlug set to a string is assignable to the Recipe type and reads back the string.
    - A Recipe object WITHOUT shareSlug reads shareSlug as undefined via optional access (no throw) — proving pre-Phase-5 docs are handled by optionality, not backfill.
    - env.import.promoteConfidence is a number strictly greater than the reviewRequired threshold (0.6) so the public bar is stricter than the "needs a human" bar (D-06).
  </behavior>
  <action>
    This is ONE atomic change committed together (mongoat two-source-of-truth: the TS type and the BSON validator are separate declarations; updating only one causes a runtime DocumentValidationFailure that compiles clean — this exact class of bug failed Phase 2/3 UAT).

    (1) In `src/modules/recipes/recipe.types.ts`, add an optional `shareSlug?: string` field to the `Recipe` interface, adjacent to `importJobId`/`confirmedAt`, with a pt-BR inline comment matching the surrounding convention: a secret, unguessable token for the public link (D-03/D-04), present only once confirmedAt is set. Do NOT add it to the `RecipeSearchHit` shape (search never needs the token).

    (2) In `src/modules/recipes/recipe.model.ts`, add `shareSlug: { bsonType: "string" }` to the BSON `properties` (mirror the `importJobId` prop shape). Do NOT add shareSlug to the schema `required` array — it stays optional. In the same file's `indexes` array, add `{ key: { shareSlug: 1 }, name: "share_slug_lookup", unique: true, sparse: true }` — copy the exact unique+sparse shape of the existing `external_id_unique` index (not the plain sparse `import_job_lookup`): unique guarantees no two recipes share a token; sparse ensures pre-Phase-5 recipes lacking the field are exempt from the unique constraint. Add a pt-BR comment above the index noting unique = token collision impossible, sparse = só receitas confirmadas (Fase 5) têm shareSlug.

    (3) In `src/config/env.ts`, inside the existing `import: { ... }` block (after `dailyLimitPro` at ~line 136, before the price table comment), add `promoteConfidence: Number(optional("IMPORT_PROMOTE_CONFIDENCE", "0.7"))`. Add a pt-BR comment explaining WHY: this is the public-promotion confidence gate (D-06), SEPARATE from `REVIEW_SCORE_THRESHOLD` (0.6, which only decides "needs a human review"); 0.7 sits above 0.6 so the public catalog bar is stricter than the review bar — cross-referencing import.confidence.ts's 0..1 scale where 0.5 = "ambiguous".

    (4) Run `npm run setup:db` (executes `src/infra/database/setup.ts` → `setupCollections()` which applies the updated `$jsonSchema` validator via collMod and provisions the `share_slug_lookup` index against the live Atlas instance). This REQUIRES the live Atlas credentials already in `.env`. Because the harness blocks .env/prod-Atlas credentials, this is a DOCUMENTED USER GATE: the executor writes all code + declares the index, but the actual `npm run setup:db` run against live/prod Atlas is captured as a manual gate in this plan's SUMMARY. This step MUST run before Plan 03's confirm-time shareSlug write, or that write fails with a Mongo document-validation error.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "share_slug_lookup" src/modules/recipes/recipe.model.ts | grep -qx 1 && grep -Ev '^[[:space:]]*//' src/modules/recipes/recipe.model.ts | grep -c '"shareSlug"' | grep -qvx 0; echo done</automated>
    <manual>npm run setup:db completes without error against live Atlas (collMod applies the new validator + share_slug_lookup unique+sparse index); confirm in the setup output that collections/validators/indexes are ready. (USER GATE — harness blocks prod-Atlas creds.)</manual>
  </verify>
  <acceptance_criteria>
    - `recipe.types.ts` declares the field: `grep -c "shareSlug" src/modules/recipes/recipe.types.ts` returns >= 1
    - `recipe.model.ts` BSON validator declares it: `grep -c "shareSlug" src/modules/recipes/recipe.model.ts` returns >= 2 (the property + the index key)
    - The index is unique AND sparse: `grep -c "share_slug_lookup" src/modules/recipes/recipe.model.ts` returns 1, and that index line contains both `unique: true` and `sparse: true`
    - shareSlug is NOT required: the BSON `required:` array line does not contain `shareSlug`
    - env has the gate: `grep -c "promoteConfidence" src/config/env.ts` returns >= 1 and `grep -c "IMPORT_PROMOTE_CONFIDENCE" src/config/env.ts` returns 1
    - `npm run typecheck` exits 0
    - setup:db has been run (manual USER GATE confirmation captured in SUMMARY)
  </acceptance_criteria>
  <done>shareSlug type + BSON validator declare the identical optional string field, the share_slug_lookup unique+sparse index exists, env.import.promoteConfidence is defined above the review threshold, and setup:db has synced the live Atlas validator + index (documented in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 2: Model-shape test — shareSlug is type-valid, absent shareSlug reads safely</name>
  <read_first>
    - src/modules/import/import-job.repository.test.ts (mongoat model-mock + vitest assertion conventions used across this repo)
    - src/modules/recipes/recipe.types.ts (the Recipe type after Task 1's shareSlug addition)
    - src/config/env.ts (the promoteConfidence field after Task 1)
  </read_first>
  <files>src/modules/recipes/recipe.model.test.ts</files>
  <action>
    Create `src/modules/recipes/recipe.model.test.ts` — a pure unit/shape-guard test (the BSON validator itself cannot be exercised without live Mongo, consistent with the repo's "mock the model, not live Mongo" convention; setup:db is NOT run from a test). Assert at the type/shape level: (a) a `Recipe`-shaped object with `shareSlug` set to a non-empty string is assignable to `Recipe` and the field reads back the string; (b) a `Recipe`-shaped object with `shareSlug` ABSENT reads back `undefined` via optional access without throwing — proving pre-Phase-5 docs are handled by optionality; (c) `env.import.promoteConfidence` is a finite number strictly greater than the review threshold `0.6` (import the real env or assert against the resolved config value) so the public bar is provably stricter than the review bar (D-06). Keep it a synchronous unit test (no Mongo, no async, no network).
  </action>
  <verify>
    <automated>npm run test -- src/modules/recipes/recipe.model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/modules/recipes/recipe.model.test.ts` passes
    - The test references the new field: `grep -c "shareSlug" src/modules/recipes/recipe.model.test.ts` returns >= 1
    - The test asserts the promotion bar is stricter than review: `grep -c "promoteConfidence" src/modules/recipes/recipe.model.test.ts` returns >= 1
  </acceptance_criteria>
  <done>A unit test proves shareSlug is type-valid, an absent shareSlug reads safely as undefined, and promoteConfidence is provably above the review threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → DB (schema validator) | The Atlas `$jsonSchema` validator gates every write; a type/validator mismatch is a runtime write failure, not a compile error |
| future public reader → recipe by token | The `shareSlug` token is the sole authorization for the public link (D-03); its DB-level uniqueness is the foundation the whole privacy model rests on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering (data integrity) | shareSlug write path | high | mitigate | Type + BSON validator changed in ONE commit + `setup:db` collMod sync before any confirm-time shareSlug write — prevents the Phase 2/3-class DocumentValidationFailure divergence |
| T-05-02 | Spoofing (token collision) | share_slug_lookup index | high | mitigate | UNIQUE index guarantees two recipes can never resolve to the same token; sparse so pre-Phase-5 recipes are exempt — collision-based cross-recipe access is structurally impossible at the DB layer |
| T-05-03 | Denial of Service (crash on read) | pre-Phase-5 Recipe docs | low | mitigate | shareSlug stays fully optional; consumers optional-access — historical recipes (no shareSlug) never crash reads |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced this plan; token generation (Plan 03) uses node:crypto (stdlib, no dependency). If any install is proposed, gate via slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `npm run typecheck` exits 0 with the shareSlug type + promoteConfidence config.
- shareSlug type (types.ts) and BSON validator (model.ts) declare the identical optional string field.
- `share_slug_lookup` index is unique AND sparse in recipe.model.ts.
- shareSlug is NOT in the BSON required array.
- `env.import.promoteConfidence` present and above the 0.6 review threshold.
- `npm run setup:db` completed against live Atlas (manual USER GATE confirmation in SUMMARY).
- `npm run test -- src/modules/recipes/recipe.model.test.ts` green.
</verification>

<success_criteria>
- Recipe.shareSlug is an optional string in both type and validator, synced in one commit.
- share_slug_lookup unique+sparse index exists for Plan 03's lookup and token-uniqueness guarantee.
- env.import.promoteConfidence exists as the dedicated public-promotion bar, stricter than review.
- The live Atlas validator + index are synced via setup:db before any Plan 03 shareSlug write.
- Absent/pre-Phase-5 shareSlug reads are proven crash-safe.
</success_criteria>

<output>
Create `.planning/phases/05-publish-promotion-full-citizenship/05-01-SUMMARY.md` when done. Record the setup:db USER GATE result explicitly (run / not-yet-run) so Plan 03 knows whether the live validator accepts shareSlug writes.
</output>
