---
phase: 04-cost-quota-gating-dedup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.ts
autonomous: true
requirements: [COST-02, COST-03]
must_haves:
  truths:
    - "env.import carries dailyLimitFree (default 3, mirroring adaptDailyLimitFree) and dailyLimitPro (default 50, anti-abuse ceiling)"
    - "env.import carries a per-unit price table (egress, ASR groq/openai, LLM in/out, embedding) all sourced from env with documented defaults"
    - "The existing env.import.maxDurationSec entry from Phase 1 is preserved, not clobbered"
  artifacts:
    - src/config/env.ts
  key_links:
    - "env.import.dailyLimitFree/Pro feed the POST /import quota gate (Plan 05); the price table feeds pipeline cost recording (Plan 06)"
  prohibitions:
    - "The new import limits/price table NEVER replace or remove the existing env.import.maxDurationSec (Phase 1) — they are ADDED to the same block"
    - "Price-per-unit values NEVER live as inline constants in pipeline.ts — they live in config only (D-08)"
    - "Import daily limits NEVER reuse the adapt env keys — they are new IMPORT_DAILY_LIMIT_FREE/PRO keys"
---

<objective>
Extend the existing `env.import` config block with the import daily quota limits (`dailyLimitFree`/`dailyLimitPro`) and the per-unit cost price table (D-08), all via the existing `Number(optional("ENV_VAR", "default"))` idiom, WITHOUT clobbering the `maxDurationSec` entry Phase 1 already placed in that block.

Purpose: COST-03 needs the free/PRO import limits in config for the submission gate (Plan 05). COST-02/D-08 requires the price table to live in config (not hardcoded in pipeline logic) so prices — which are LOW-confidence estimates (RESEARCH A1–A4) — can be corrected without a code change.
Output: an expanded `env.import` block with limits + a documented, source-dated price table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-quota-gating-dedup/04-CONTEXT.md
@.planning/phases/04-cost-quota-gating-dedup/04-RESEARCH.md
@.planning/phases/04-cost-quota-gating-dedup/04-PATTERNS.md

# File being modified (read the WHOLE existing import + anthropic blocks before editing)
@src/config/env.ts
</context>

<artifacts_this_phase_produces>
- (Plan 01) quota functions + import_usage model
- (Plan 02) expanded costCents shape + dedup_lookup index
- `env.import.dailyLimitFree/Pro` + price table (this plan)
- (Plan 04) findExistingSuccessfulImport
- (Plan 05) POST /import gate + dedup-hit 200 + PasteLinkButton branch
- (Plan 06) pipeline per-stage cost recording + refund-in-failJob
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add import daily limits + price table to env.import (preserve maxDurationSec)</name>
  <read_first>
    - src/config/env.ts (the EXISTING env.import block — currently only maxDurationSec, ~lines 125-130 — and the env.anthropic adaptDailyLimitFree/Pro block ~lines 54-59 to mirror the idiom)
  </read_first>
  <files>src/config/env.ts</files>
  <action>
    Edit the EXISTING `import: { ... }` block in `src/config/env.ts` (the one that already has `maxDurationSec: Number(optional("IMPORT_MAX_DURATION_SEC", "600"))`). ADD new keys to that same block — do NOT create a second `import:` block and do NOT remove `maxDurationSec` (PATTERNS.md flag: the entry already exists from Phase 1).

    Add the quota limits, mirroring the `env.anthropic.adaptDailyLimitFree/Pro` idiom exactly: `dailyLimitFree: Number(optional("IMPORT_DAILY_LIMIT_FREE", "3"))` (D-02: mirrors adaptDailyLimitFree=3) and `dailyLimitPro: Number(optional("IMPORT_DAILY_LIMIT_PRO", "50"))` (D-02: dedicated high anti-abuse ceiling).

    Add the price table (D-08 — cents per unit, all via `Number(optional(...))`): `priceCentsPerGbEgress` (default "9"), `priceCentsPerAsrMinuteGroq` (default "0.0667"), `priceCentsPerAsrMinuteOpenai` (default "0.6"), `priceCentsPerMtokLlmInput` (default "300"), `priceCentsPerMtokLlmOutput` (default "1500"), `priceCentsPerMtokEmbedding` (default "6"), using env keys `IMPORT_PRICE_CENTS_PER_GB_EGRESS`, `IMPORT_PRICE_CENTS_PER_ASR_MIN_GROQ`, `IMPORT_PRICE_CENTS_PER_ASR_MIN_OPENAI`, `IMPORT_PRICE_CENTS_PER_MTOK_LLM_IN`, `IMPORT_PRICE_CENTS_PER_MTOK_LLM_OUT`, `IMPORT_PRICE_CENTS_PER_MTOK_EMBED`.

    Add a pt-BR comment block above the price table documenting: source date (2026-07-02), that these are LOW-confidence estimates for operational review (COST-02) and NOT billing-critical, that a human should spot-check the Anthropic Sonnet 4.5 figure before launch (RESEARCH A2 introductory-vs-standard price ambiguity), and that the download/egress figure is a rough proxy — the video downloads to the Render worker disk (yt-dlp), it does NOT transit S3, so treat raw bytes as the durable ground truth and cents as best-effort (RESEARCH A4 / Open Question 1). Keep the comment prose free of any acceptance-criteria literal that would be negative-grepped.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "dailyLimitFree" src/config/env.ts && grep -c "maxDurationSec" src/config/env.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dailyLimitFree" src/config/env.ts` returns >= 1 AND `grep -c "dailyLimitPro" src/config/env.ts` returns >= 1
    - The Phase 1 entry is preserved: `grep -c "maxDurationSec" src/config/env.ts` returns 1
    - Only ONE import block exists: `grep -c "^  import: {" src/config/env.ts` returns 1
    - The price table is present: `grep -c "priceCentsPerMtokLlmInput" src/config/env.ts` returns >= 1 AND `grep -c "priceCentsPerAsrMinuteGroq" src/config/env.ts` returns >= 1
    - New env keys use the IMPORT_ prefix (not the ADAPT_ keys): `grep -c "IMPORT_DAILY_LIMIT_FREE" src/config/env.ts` returns >= 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>env.import carries the import daily limits and the documented price table alongside the preserved maxDurationSec entry, all via the standard optional() idiom, and typechecks.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0.
- `env.import` has exactly one block containing maxDurationSec (preserved) + dailyLimitFree/Pro + the six price-table keys.
- No price-per-unit constant is hardcoded outside config (enforced downstream in Plan 06).
</verification>

<success_criteria>
- Import daily limits (free=3, pro=50) and the per-unit price table live in env.import.
- The Phase 1 maxDurationSec entry is untouched.
- Price defaults are documented as source-dated, LOW-confidence, config-updatable estimates.
</success_criteria>

<output>
Create `.planning/phases/04-cost-quota-gating-dedup/04-03-SUMMARY.md` when done.
</output>
