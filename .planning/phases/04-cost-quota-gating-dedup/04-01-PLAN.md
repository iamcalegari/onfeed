---
phase: 04-cost-quota-gating-dedup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/usage/import-usage.model.ts
  - src/modules/usage/usage.repository.ts
  - src/modules/usage/usage.repository.test.ts
  - src/modules/usage/README.md
autonomous: true
requirements: [COST-01, COST-03]
must_haves:
  truths:
    - "consumeDailyImportQuota atomically increments a per-{userId,day} counter and returns allowed=false once count exceeds the plan limit"
    - "refundDailyImportQuota decrements the counter for a specific reserved day (never today's day blindly)"
    - "import quota lives in its own import_usage collection, fully isolated from adapt_usage"
  artifacts:
    - src/modules/usage/import-usage.model.ts
    - src/modules/usage/usage.repository.ts
    - src/modules/usage/usage.repository.test.ts
  key_links:
    - "ImportUsageModel unique index {userId, day} is the atomicity boundary for COST-01 race-safety"
  prohibitions:
    - "import quota is NEVER a discriminator field on AdaptUsageModel — it is a dedicated import_usage collection (Open Question 2 resolved: dedicated collection)"
    - "refundDailyImportQuota NEVER uses upsert (the day-doc must already exist because quota was reserved before the job could fail)"
    - "consumeDailyImportQuota NEVER introduces a second concurrency mechanism (no Redis, no in-memory counter) — it reuses the mongoat Model.update $inc upsert primitive"
---

<objective>
Build the atomic per-day import quota primitive that the POST /import gate (Plan 05) and the pipeline refund path (Plan 06) depend on. Mirror the existing `consumeDailyAdaptQuota` exactly, but against a NEW dedicated `import_usage` collection (per RESEARCH Open Question 2 — zero risk to the production adapt quota), and add the one genuinely novel piece: a refund (negative `$inc`) with no upsert, keyed by a caller-supplied day.

Purpose: COST-01 requires the daily free-tier import quota to be reserved atomically at submission; the `$inc` upsert on a uniquely-indexed `{userId, day}` document IS the race-safety mitigation. COST-03 reuses this quota to gate free vs. PRO.
Output: `ImportUsageModel` (new model), `consumeDailyImportQuota` + `refundDailyImportQuota` in `usage.repository.ts`, first-ever test coverage for that repository file, and an updated usage module README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-quota-gating-dedup/04-CONTEXT.md
@.planning/phases/04-cost-quota-gating-dedup/04-PATTERNS.md
@.planning/phases/04-cost-quota-gating-dedup/04-VALIDATION.md

# Analogs (read before implementing)
@src/modules/usage/usage.repository.ts
@src/modules/usage/usage.model.ts
@src/modules/import/import-job.repository.test.ts
</context>

<artifacts_this_phase_produces>
This plan contributes to the phase-wide artifact set:
- `consumeDailyImportQuota(userId, limit)` — atomic reservation (this plan)
- `refundDailyImportQuota(userId, day)` — atomic refund (this plan)
- `import_usage` collection / `ImportUsageModel` (this plan)
- (Plan 02) expanded `ImportJob.costCents` nested per-stage shape
- (Plan 03) `IMPORT_DAILY_LIMIT_FREE/PRO` + price table config in `env.import`
- (Plan 04) `findExistingSuccessfulImport` dedup lookup in `import-job.repository.ts`
- (Plan 05) dedup + quota gate wired into `POST /import`, dedup-hit 200 contract, PasteLinkButton branch
- (Plan 06) per-stage cost recording + refund-in-failJob in `pipeline.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the dedicated import_usage model</name>
  <read_first>
    - src/modules/usage/usage.model.ts (the AdaptUsageModel analog — copy structure verbatim, rename)
    - src/modules/import/import-job.model.ts (mongoat Model + indexes convention reference)
  </read_first>
  <files>src/modules/usage/import-usage.model.ts</files>
  <behavior>
    - The model exposes only METHODS.UPDATE and METHODS.FIND (mirrors AdaptUsageModel's allowedMethods).
    - The collection name is import_usage (distinct from adapt_usage).
    - A unique index named user_day_unique on { userId: 1, day: 1 } exists in the indexes array.
    - The BSON schema requires userId, day, count, insertedAt, updatedAt with the same bsonTypes as AdaptUsage.
  </behavior>
  <action>
    Create `src/modules/usage/import-usage.model.ts` as a file-for-file structural mirror of `src/modules/usage/usage.model.ts`. Rename the interface `AdaptUsage` to `ImportUsage`, the exported model `AdaptUsageModel` to `ImportUsageModel`, and `collectionName: "adapt_usage"` to `collectionName: "import_usage"`. Keep the interface fields identical (`_id?`, `userId`, `day` as YYYY-MM-DD UTC string, `count`, `insertedAt`, `updatedAt`). Keep `allowedMethods: [METHODS.UPDATE, METHODS.FIND]`, the same `documentDefaults`, and the unique index `{ key: { userId: 1, day: 1 }, name: "user_day_unique", unique: true }`. Per D-02 and RESEARCH Open Question 2, this is a DEDICATED collection — do NOT add a `kind` discriminator to `AdaptUsageModel` and do NOT touch `usage.model.ts`. Ensure the model is registered by the module barrel so `setup:db` provisions it (verify whether `src/modules/usage/` is imported via `@/modules/index.js`; if the usage module already exports/registers `AdaptUsageModel` through the barrel, register `ImportUsageModel` the same way).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import_usage" src/modules/usage/import-usage.model.ts` returns >= 1
    - `grep -c "user_day_unique" src/modules/usage/import-usage.model.ts` returns 1
    - `grep -c "ImportUsageModel" src/modules/usage/import-usage.model.ts` returns >= 1
    - `import-usage.model.ts` does NOT contain the token adapt_usage (this is a distinct collection): `grep -v '^ *//' src/modules/usage/import-usage.model.ts | grep -c adapt_usage` returns 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>ImportUsageModel exists as a dedicated import_usage collection with a unique {userId,day} index, registered for setup:db, and typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add consumeDailyImportQuota + refundDailyImportQuota (Wave 0 tests first)</name>
  <read_first>
    - src/modules/usage/usage.repository.ts (consumeDailyAdaptQuota / getDailyAdaptCount — copy the atomic-upsert structure)
    - src/modules/import/import-job.repository.test.ts (mongoat model-mock + call-shape assertion conventions)
  </read_first>
  <files>src/modules/usage/usage.repository.ts, src/modules/usage/usage.repository.test.ts</files>
  <behavior>
    - Wave 0 (RED first): create src/modules/usage/usage.repository.test.ts — the first test coverage for this repository file.
    - consumeDailyImportQuota returns allowed=true while count <= limit and allowed=false once the mocked update() resolves count = limit + 1 (boundary).
    - consumeDailyImportQuota calls ImportUsageModel.update with { userId, day } filter, an update doc containing $inc: { count: 1 } and $setOnInsert.insertedAt, and options { upsert: true }.
    - refundDailyImportQuota calls ImportUsageModel.update with $inc: { count: -1 } and NO upsert option.
    - Isolation: the {userId, day} filter is passed through unchanged (per-user, per-day scoping).
  </behavior>
  <action>
    FIRST write `src/modules/usage/usage.repository.test.ts` (Wave 0 — no existing test file). Mock the new model via `vi.mock("./import-usage.model.js", () => ({ ImportUsageModel: { update: vi.fn(), find: vi.fn() } }))`, mirroring the mock/assertion idiom in `import-job.repository.test.ts`. Cover: (a) `consumeDailyImportQuota` returns `allowed: true` when mocked `update()` resolves `{ count: 2 }` and limit is 3; (b) a `boundary` case (test name must contain the word "boundary") where mocked `update()` resolves `{ count: 4 }` and limit is 3 → `allowed: false`; (c) assert the `update()` first arg is `{ userId, day }` and the second arg contains `$inc: { count: 1 }` with options `{ upsert: true }`; (d) `refundDailyImportQuota` calls `update()` with `$inc: { count: -1 }` and asserts the third options arg does NOT set `upsert: true`. Then implement both functions in `src/modules/usage/usage.repository.ts`: `consumeDailyImportQuota(userId, limit): Promise<QuotaResult>` as a 1:1 copy of `consumeDailyAdaptQuota` but importing and calling `ImportUsageModel` instead of `AdaptUsageModel` (reuse the existing exported `QuotaResult` interface — do NOT define a new one); and `refundDailyImportQuota(userId: string, day: string): Promise<void>` doing `ImportUsageModel.update({ userId, day }, { $inc: { count: -1 }, $set: { updatedAt: new Date() } })` with NO third options argument (no upsert — the day-doc must already exist because quota was reserved before the job could fail, per RESEARCH Pattern 2). Compute `day` inside `consumeDailyImportQuota` as `new Date().toISOString().slice(0, 10)` (matching the adapt idiom). Add a short Portuguese doc comment on each new function in the same style as the existing ones, and note refund takes an explicit `day` because it refunds the day the slot was RESERVED, not today.
  </action>
  <verify>
    <automated>npm run test -- src/modules/usage/usage.repository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/modules/usage/usage.repository.test.ts` passes (all cases green)
    - The test file contains a case whose name includes "boundary": `grep -c "boundary" src/modules/usage/usage.repository.test.ts` returns >= 1
    - `grep -c "consumeDailyImportQuota" src/modules/usage/usage.repository.ts` returns >= 1
    - `grep -c "refundDailyImportQuota" src/modules/usage/usage.repository.ts` returns >= 1
    - refund uses a negative increment: `grep -c '\$inc: { count: -1 }' src/modules/usage/usage.repository.ts` returns >= 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Both quota functions exist against ImportUsageModel, the reservation caps at the limit boundary, refund decrements without upsert, and the new test suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Update usage module README (Obsidian style)</name>
  <read_first>
    - src/modules/usage/README.md (if it exists — otherwise use another module README as the Obsidian-style template, e.g. src/modules/import/README.md)
  </read_first>
  <files>src/modules/usage/README.md</files>
  <action>
    Create or update `src/modules/usage/README.md` in the project's Obsidian style (frontmatter, wikilinks, callouts) per the project convention (user memory: README Obsidian por módulo). Document the two quota families now living in this module: adapt quota (`AdaptUsageModel` / `adapt_usage` / `consumeDailyAdaptQuota`) and the NEW import quota (`ImportUsageModel` / `import_usage` / `consumeDailyImportQuota` + `refundDailyImportQuota`). Explain that they are deliberately SEPARATE collections (zero cross-contamination), that the `{userId, day}` unique index is the atomicity boundary for COST-01, and that refund is keyed by the reserved day, not today. All prose in pt-BR. Do NOT include implementation code blocks that echo any acceptance-criteria literal.
  </action>
  <verify>
    <automated>test -f src/modules/usage/README.md && grep -c "import_usage" src/modules/usage/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/usage/README.md` exists
    - `grep -c "import_usage" src/modules/usage/README.md` returns >= 1
    - `grep -c "consumeDailyImportQuota" src/modules/usage/README.md` returns >= 1
  </acceptance_criteria>
  <done>The usage module README documents both quota families in Obsidian style, pt-BR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → DB (quota counter) | The atomic `$inc` upsert is the concurrency boundary; two racing reservations must not both win a slot past the limit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering / Elevation (resource abuse) | `consumeDailyImportQuota` counter | high | mitigate | Atomic `$inc` upsert on the `{userId, day}` UNIQUE index (`user_day_unique`) — race-safe per mongoat's `findOneAndUpdate`-backed `update()`; overshoot bounded to +1, never unbounded, matching the accepted adapt-quota tradeoff |
| T-04-02 | Information Disclosure | import quota isolation | medium | mitigate | Dedicated `import_usage` collection — a bug in import quota can never read or corrupt another user's or the adapt system's counts; the `{userId, day}` filter scopes every read/write to one user |
| T-04-03 | Repudiation (refund abuse) | `refundDailyImportQuota` | low | accept | Refund is never client-triggerable in this plan (it is called only by the pipeline in Plan 06); no new client-facing attack surface is introduced here |
</threat_model>

<verification>
- `npm run test -- src/modules/usage/usage.repository.test.ts` green (atomic $inc, boundary cap, refund shape).
- `npm run typecheck` exits 0.
- `import-usage.model.ts` declares a UNIQUE `{userId, day}` index and the dedicated `import_usage` collection.
- No modification to `usage.model.ts` or `adapt_usage` (verify via git diff — only new files + `usage.repository.ts` additions).
</verification>

<success_criteria>
- ImportUsageModel provisions a dedicated import_usage collection with a unique {userId,day} index.
- consumeDailyImportQuota reserves atomically and caps at the limit; refundDailyImportQuota decrements the reserved day without upsert.
- First test coverage for usage.repository.ts is green.
- Usage module README updated (Obsidian, pt-BR).
</success_criteria>

<output>
Create `.planning/phases/04-cost-quota-gating-dedup/04-01-SUMMARY.md` when done.
</output>
