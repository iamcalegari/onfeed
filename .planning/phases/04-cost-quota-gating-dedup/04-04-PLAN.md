---
phase: 04-cost-quota-gating-dedup
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/modules/import/import-job.repository.ts
  - src/modules/import/import-job.repository.test.ts
autonomous: true
requirements: [CAP-03, COST-02]
must_haves:
  truths:
    - "findExistingSuccessfulImport(userId, normalizedUrl) returns a prior ready_for_review job for the SAME user and same normalizedUrl, or null"
    - "The lookup folds userId into the Mongo filter itself (owner-scoped in the query, never fetch-then-compare)"
    - "The lookup matches only status ready_for_review — failed jobs never match (D-05), other users never match (D-01)"
    - "The match is permanent — the lookup applies NO time/TTL filter, so a successfully-imported URL dedups indefinitely (D-06)"
  artifacts:
    - src/modules/import/import-job.repository.ts
    - src/modules/import/import-job.repository.test.ts
  key_links:
    - "findExistingSuccessfulImport is the CAP-03 dedup lookup that POST /import (Plan 05) calls before the quota gate"
  prohibitions:
    - "The dedup lookup NEVER omits the userId filter — a {normalizedUrl,status}-only query would leak another user's private import (IDOR / D-01)"
    - "The dedup lookup NEVER matches status:failed (D-05 — reimporting a failed URL is a legitimate retry)"
    - "The lookup NEVER fetches by normalizedUrl then compares userId in application code — userId is folded into the Mongo filter (getImportJob idiom)"
---

<objective>
Add the per-user, terminal-success dedup lookup `findExistingSuccessfulImport(userId, normalizedUrl)` to `import-job.repository.ts`, folding `userId` into the Mongo filter itself (the established IDOR-safe `getImportJob` idiom), matching only `status: "ready_for_review"`. Extend the existing repository test with dedup cases plus a costCents-shape read case (using Plan 02's expanded type).

Purpose: CAP-03 requires duplicate submissions of the same normalized URL to reuse the existing result. D-01 locks this to per-user (owner-scoped) dedup; D-05 excludes failed jobs; D-06 makes the match permanent (no TTL / no cache window — the lookup applies no time filter, so a successfully-imported URL dedups indefinitely). This lookup is the first guard in `POST /import` (Plan 05). Security V4: the `userId` predicate is mandatory or a same-URL submission leaks another user's private import.
Output: `findExistingSuccessfulImport` in the repository + dedup and costCents test cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-quota-gating-dedup/04-CONTEXT.md
@.planning/phases/04-cost-quota-gating-dedup/04-RESEARCH.md
@.planning/phases/04-cost-quota-gating-dedup/04-PATTERNS.md

# File being modified + its analog (read before implementing)
@src/modules/import/import-job.repository.ts
@src/modules/import/import-job.repository.test.ts
@src/modules/import/import-job.types.ts

# Depends on 04-02 (expanded costCents type)
@.planning/phases/04-cost-quota-gating-dedup/04-02-PLAN.md
</context>

<artifacts_this_phase_produces>
- (Plan 01) quota functions + import_usage model
- (Plan 02) expanded costCents shape + dedup_lookup index
- (Plan 03) env.import limits + price table
- `findExistingSuccessfulImport` dedup lookup (this plan)
- (Plan 05) POST /import gate + dedup-hit 200 + PasteLinkButton branch
- (Plan 06) pipeline per-stage cost recording + refund-in-failJob
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add findExistingSuccessfulImport (owner-scoped dedup lookup) with tests</name>
  <read_first>
    - src/modules/import/import-job.repository.ts (getImportJob(jobId, userId) — the owner-scoped-filter idiom to copy; the ImportJobModel.find usage)
    - src/modules/import/import-job.repository.test.ts (existing mongoat model-mock + call-shape assertion conventions)
    - src/modules/import/import-job.types.ts (ImportJobStatus — confirm there is no "confirmed" status; ready_for_review is the only terminal success)
  </read_first>
  <files>src/modules/import/import-job.repository.ts, src/modules/import/import-job.repository.test.ts</files>
  <behavior>
    - findExistingSuccessfulImport returns the mocked job when ImportJobModel.find resolves a ready_for_review job for {userId, normalizedUrl}.
    - It returns null when find resolves null (no prior successful import).
    - The find filter passed to ImportJobModel.find contains userId AND normalizedUrl AND status: "ready_for_review" (all three) — asserted from the mock call args.
    - The filter NEVER contains status: "failed" and NEVER omits userId (IDOR guard).
  </behavior>
  <action>
    FIRST extend `src/modules/import/import-job.repository.test.ts` (Wave 0 additions, tests before implementation): add cases for `findExistingSuccessfulImport` mirroring the existing model-mock idiom (mock `ImportJobModel.find`). Assert: (a) a HIT returns the resolved job; (b) a MISS (find resolves null) returns null; (c) the filter object passed to `ImportJobModel.find` contains `userId`, `normalizedUrl`, and `status: "ready_for_review"` — extract the first call arg and assert all three keys (this is the IDOR-safety assertion: userId is folded into the query); (d) an explicit assertion that the filter's `status` is NOT `"failed"` (D-05). ALSO add a `costCents`-shape read case (test name should include "costCents"): construct a mocked ImportJob with the new nested `costCents` (`download.cents`, `extraction.inputTokens`, `totalCents`) and assert an optional-chained read returns the expected values and that an absent costCents reads as undefined without throwing (Pitfall 4 — depends on Plan 02's expanded type).

    THEN implement `findExistingSuccessfulImport(userId: string, normalizedUrl: string): Promise<ImportJob | null>` in `import-job.repository.ts`, copying the `getImportJob(jobId, userId)` owner-scoped idiom: `const job = await ImportJobModel.find({ userId, normalizedUrl, status: "ready_for_review" } as never); return (job as ImportJob | null) ?? null;`. Add a pt-BR doc comment noting: per-user dedup (D-01), ready_for_review is the ONLY terminal-success state (there is no "confirmed" ImportJobStatus — confirmation lives on Recipe.confirmedAt, orthogonal to ImportJob.status), and failed never matches (D-05, legitimate retry). Do NOT fetch by normalizedUrl and compare userId afterward — fold userId into the filter (IDOR discipline established by getImportJob / listMyImportedRecipes).
  </action>
  <verify>
    <automated>npm run test -- src/modules/import/import-job.repository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/modules/import/import-job.repository.test.ts` passes (dedup HIT/MISS, filter-shape, costCents cases green)
    - `grep -c "findExistingSuccessfulImport" src/modules/import/import-job.repository.ts` returns >= 1
    - The lookup matches ready_for_review: `grep -c 'ready_for_review' src/modules/import/import-job.repository.ts` returns >= 1
    - A test asserts the filter includes userId (IDOR guard): the test file references both `findExistingSuccessfulImport` and `userId` in an assertion — `grep -c "findExistingSuccessfulImport" src/modules/import/import-job.repository.test.ts` returns >= 1
    - A costCents-shape test case exists: `grep -c "costCents" src/modules/import/import-job.repository.test.ts` returns >= 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>findExistingSuccessfulImport does an owner-scoped ready_for_review lookup (userId folded into the filter), never matches failed or other users, and the repository test covers dedup HIT/MISS/filter-shape plus the expanded costCents read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API → DB (dedup lookup) | A same-URL submission by user B must never surface user A's private import; the query itself must scope by userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure (IDOR / cross-user leak) | `findExistingSuccessfulImport` | high | mitigate | `userId` is folded into the Mongo filter itself (`{ userId, normalizedUrl, status }`) — never a global `normalizedUrl`-only lookup, never fetch-then-compare; matches the established `getImportJob(jobId, userId)` idiom (D-01) |
| T-04-08 | Tampering (stale/failed reuse) | dedup status match | medium | mitigate | The lookup matches ONLY `status: "ready_for_review"` — a `failed` job never dedups (D-05), so a retry of a previously-failed URL correctly re-runs the pipeline |
</threat_model>

<verification>
- `npm run test -- src/modules/import/import-job.repository.test.ts` green (HIT, MISS, filter-shape incl. userId, costCents read).
- `npm run typecheck` exits 0.
- findExistingSuccessfulImport folds userId into the filter and matches only ready_for_review.
</verification>

<success_criteria>
- findExistingSuccessfulImport is the owner-scoped, ready_for_review-only dedup lookup for CAP-03.
- The lookup can never leak another user's private import (userId in the filter) and can never dedup a failed job.
- Repository tests cover dedup behavior and the expanded costCents read.
</success_criteria>

<output>
Create `.planning/phases/04-cost-quota-gating-dedup/04-04-SUMMARY.md` when done.
</output>
