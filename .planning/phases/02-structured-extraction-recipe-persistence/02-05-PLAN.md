---
phase: 02-structured-extraction-recipe-persistence
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/infra/video/pipeline.ts
  - src/modules/import/import.recipe-mapping.ts
  - src/modules/import/import.recipe-mapping.test.ts
  - src/workers/import-worker.test.ts
  - src/modules/import/README.md
autonomous: true
requirements: [EXT-01, EXT-03, EXT-04, EXT-05]
must_haves:
  truths:
    - "The pipeline's `extracting` stage (today a stub) calls extractImportedRecipe with the job's transcript+caption+noSpeechDetected, then computeConfidence, then maps to a Recipe and persists it via persistExtractedRecipe (EXT-01/EXT-03/EXT-04)"
    - "The persisted imported recipe has source='imported', visibility='private', importJobId set, sourceMeta from the ImportJob, grounding stored, and confidenceScore set (EXT-02/EXT-04)"
    - "Ingredients go through the existing resolveCanonicalForIngestion path inside persistExtractedRecipe — no duplicate canonicalization logic in the import module (EXT-03)"
    - "The job ALWAYS lands ImportJob.status='ready_for_review' after a successful extraction — there is NO code path from extraction to a public/published status (EXT-05); reviewRequired + confidenceScore + recipeId are written onto the ImportJob"
    - "noSpeechDetected propagates into the extraction input and (via computeConfidence) forces reviewRequired — a silent-video import cannot silently produce a trusted recipe"
    - "An extraction failure sets ImportJob.status='failed' with failureReason='extraction_failed' (never a partial/false-success), and never leaves a dangling half-written recipe"
  artifacts:
    - "src/infra/video/pipeline.ts (extracting stage wired to real extraction+confidence+persist)"
    - "src/modules/import/import.recipe-mapping.ts (mapExtractedToRecipe: ExtractedImportedRecipe + ImportJob → persistExtractedRecipe input)"
    - "src/modules/import/import.recipe-mapping.test.ts + extended src/workers/import-worker.test.ts"
---

<objective>
Close the loop: turn the Phase-1 `extracting` stub in `src/infra/video/pipeline.ts` into the real extraction → confidence → persistence → `ready_for_review` sequence, wiring together everything Plans 01-04 built. This is the plan that makes EXT-05's structural guarantee real: a successful extraction always lands `ready_for_review` (never a direct public status), with `reviewRequired`/`confidenceScore`/`recipeId` recorded, and a failure lands `failed`/`extraction_failed`.

Purpose: EXT-01 (structured recipe produced), EXT-03 (canonicalization reused via persistExtractedRecipe), EXT-04 (embedded + owner-searchable via the persist path + Plan 03's owner-scoping), EXT-05 (mandatory-review routing enforced structurally). The extraction, confidence gate, owner-scoped search, and schema all already exist from Plans 01-04 — this plan is the integration + mapping + the persistence contract, plus the pipeline-level test that proves ready_for_review is the only success terminal.
Output: a working `extracting` stage, an import→Recipe mapping function, and tests proving the ready_for_review guarantee and the failure path.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map an extracted+scored import to a Recipe persistence input</name>
  <read_first>
    - src/modules/import/import.extraction.ts (ExtractedImportedRecipe shape from Plan 02)
    - src/modules/import/import.confidence.ts (ConfidenceResult from Plan 04)
    - src/modules/recipes/recipe.generation.ts (persistExtractedRecipe signature + IngestOptions extended in Plan 01)
    - src/modules/recipes/recipe.ingestion.ts (the ingredient/persist loop the mapping must satisfy — RESEARCH Canonicalization Reuse excerpt)
    - src/modules/import/import-job.types.ts (ImportJob fields: transcript/caption/sourceMeta/noSpeechDetected/recipeId/reviewRequired/confidenceScore)
    - .planning/phases/02-structured-extraction-recipe-persistence/02-PATTERNS.md (persistExtractedRecipe / IngestOptions sections)
  </read_first>
  <files>src/modules/import/import.recipe-mapping.ts</files>
  <behavior>
    - Exports `mapExtractedToRecipe(extracted: ExtractedImportedRecipe, job: ImportJob, confidence: ConfidenceResult)` returning the exact input shape persistExtractedRecipe expects (extracted-recipe object + IngestOptions), NOT persisting itself.
    - IngestOptions carries visibility:'private', importJobId:job._id, sourceMeta from job.sourceMeta (+ platform), and the grounding blob (title/ingredient/step grounding + nutrition:'inferred' hardcoded per D-10) + confidenceScore + reviewRequired.
    - The ingredient list preserves raw/name/quantity/unit/core so the existing canonicalization loop consumes it unchanged (EXT-03); ambiguous quantities remain as-extracted (null quantity + literal unit).
  </behavior>
  <action>
    Create src/modules/import/import.recipe-mapping.ts. Implement mapExtractedToRecipe(extracted, job, confidence): build the recipe object structurally identical to what recipe.extraction.ts produces for persistExtractedRecipe (title, intro, ingredients[raw/name/quantity/unit/core], steps, nutrition, equipment/occasions if present), and build the IngestOptions with visibility:'private', importJobId: String(job._id), sourceMeta: { platform: job.platform, authorHandle: job.sourceMeta?.authorHandle, authorUrl: job.sourceMeta?.authorUrl, sourceUrl: job.normalizedUrl }, grounding: { titleGrounding, ingredientGrounding (from each ingredient's quantityGrounding), stepGrounding, nutrition:'inferred', sourceDivergence }, confidenceScore: confidence.score, reviewRequired: confidence.reviewRequired — using the `...(value && { field })` idiom for optionals (Mongoat gotcha, no explicit undefined). Return { recipe, options }. Do NOT re-implement canonicalization or embedding — that stays inside persistExtractedRecipe.
  </action>
  <verify>
    <automated>npm run test -- src/modules/import/import.recipe-mapping.test.ts</automated>
  </verify>
  <done>mapExtractedToRecipe returns a persistExtractedRecipe-compatible {recipe, options}; options carry visibility private, importJobId, sourceMeta, grounding (nutrition inferred), confidenceScore, reviewRequired; ambiguous quantities preserved; test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the extracting stage in pipeline.ts + failure path</name>
  <read_first>
    - src/infra/video/pipeline.ts (current processImportJob; the `extracting` stub between transcription and ready_for_review; the two-call updateImportJobStatus idiom; how keyframeUrl/status are written near lines 205-215)
    - src/modules/import/import.extraction.ts (extractImportedRecipe)
    - src/modules/import/import.confidence.ts (computeConfidence)
    - src/modules/import/import.recipe-mapping.ts (mapExtractedToRecipe from Task 1)
    - src/modules/recipes/recipe.generation.ts (persistExtractedRecipe — returns the created recipe / its id)
    - src/modules/import/import-job.repository.ts (updateImportJobStatus signature + fields recipeId/reviewRequired/confidenceScore)
  </read_first>
  <files>src/infra/video/pipeline.ts</files>
  <behavior>
    - In processImportJob, the `extracting` stage: updateImportJobStatus(id, { status:'extracting' }); call extractImportedRecipe({ transcript, caption, noSpeechDetected }); computeConfidence(extracted, { noSpeechDetected }); mapExtractedToRecipe → persistExtractedRecipe (which canonicalizes + embeds + inserts the private recipe); then the final status write becomes updateImportJobStatus(id, { status:'ready_for_review', recipeId, reviewRequired: confidence.reviewRequired, confidenceScore: confidence.score, keyframeUrl }).
    - There is NO branch that sets a public/published status from here — ready_for_review is the only success terminal (EXT-05).
    - Exception classification (EXT-05 structural guarantee — every failure lands failed/extraction_failed, never a dangling `extracting`): ALL extraction-layer errors — zod/schema validation failure, LLM `parsed_output` null / non-`end_turn` stop_reason, and mapping errors — map to `failed`/`extraction_failed` and are NOT retried (a re-run would just fail identically). `persistExtractedRecipe` is atomic at the app level: it either fully inserts the recipe or throws before linking a recipeId — there is no half-written recipe referenced by the job. A persistence throw likewise maps to `failed`/`extraction_failed` with no recipeId set. It is structurally impossible to leave the job stuck in `extracting`.
    - On extraction/persist throw: updateImportJobStatus(id, { status:'failed', failedStep:'extracting', failureReason:'extraction_failed', errorMessage }); the two-layer temp cleanup (Phase 1) still runs; no half-written recipe is left referenced by the job.
  </behavior>
  <action>
    In src/infra/video/pipeline.ts, replace the stubbed `extracting` step (which currently passes straight to ready_for_review) with: set status 'extracting'; const extracted = await extractImportedRecipe({ transcript: job.transcript, caption: job.caption, noSpeechDetected: job.noSpeechDetected ?? false }); const confidence = computeConfidence(extracted, { noSpeechDetected: job.noSpeechDetected ?? false }); const { recipe, options } = mapExtractedToRecipe(extracted, job, confidence); const created = await persistExtractedRecipe(recipe, options); then fold recipeId=String(created._id), reviewRequired, confidenceScore into the existing final ready_for_review updateImportJobStatus call (merge with keyframeUrl rather than a separate write, matching the stage idiom). Wrap the extraction+persist in a single try/catch: on ANY error (zod validation, LLM parsed_output-null/stop_reason, mapping error, or a persistExtractedRecipe throw) call updateImportJobStatus with status 'failed', failedStep 'extracting', failureReason 'extraction_failed', and a user-safe errorMessage — these are treated as non-retryable (do not re-enqueue). Because persistExtractedRecipe either fully inserts or throws (no partial recipe), the failure branch never links a recipeId. Re-throw or return per the existing pipeline error convention so the outcome telemetry + two-layer temp cleanup still fire. Do not add a retry branch here — SQS redelivery of a deterministic extraction failure would just repeat it; the DLQ (Phase 1) absorbs poison messages. Import extractImportedRecipe, computeConfidence, mapExtractedToRecipe, persistExtractedRecipe. Do NOT log the full transcript or LLM payload (CONCERNS.md logging discipline).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>pipeline.ts extracting stage calls extract→confidence→map→persist and lands ready_for_review with recipeId/reviewRequired/confidenceScore; failure sets failed/extraction_failed; no public-status path exists; transcript not logged; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pipeline integration test (ready_for_review guarantee) + README</name>
  <read_first>
    - src/workers/import-worker.test.ts (existing mock harness for processImportJob — mocks adapters + ImportJobModel; the pattern to extend)
    - src/infra/video/pipeline.ts (the wired stage from Task 2)
    - .planning/phases/02-structured-extraction-recipe-persistence/02-VALIDATION.md (EXT-05 pipeline row)
    - src/modules/import/README.md (Obsidian-style module doc to update)
  </read_first>
  <files>src/workers/import-worker.test.ts, src/modules/import/import.recipe-mapping.test.ts, src/modules/import/README.md</files>
  <behavior>
    - With all adapters mocked (extractImportedRecipe returns a fixture, persistExtractedRecipe returns a fake recipe id), processImportJob lands status 'ready_for_review' and writes recipeId/reviewRequired/confidenceScore — and NEVER any public/published status.
    - A mocked extraction throw drives status 'failed' + failureReason 'extraction_failed'.
    - A noSpeechDetected job still reaches ready_for_review but with reviewRequired true (integration of Plan 04's override).
    - import.recipe-mapping.test.ts asserts the mapping output shape from Task 1.
    - README documents the extraction stage + the ready_for_review/reviewRequired contract (Obsidian style).
  </behavior>
  <action>
    Extend src/workers/import-worker.test.ts (or add pipeline-focused cases) mocking @/modules/import/import.extraction.js, @/modules/import/import.confidence.js (or let the real pure fn run on a fixture), @/modules/recipes/recipe.generation.js persistExtractedRecipe, and ImportJobModel, following the existing mock style. Assert: (1) happy path → updateImportJobStatus called with status 'ready_for_review' and recipeId/reviewRequired/confidenceScore, and assert no call ever passes a 'public'/'published' status; (2) extraction throw → status 'failed', failureReason 'extraction_failed'; (3) noSpeechDetected true → ready_for_review with reviewRequired true. Create src/modules/import/import.recipe-mapping.test.ts asserting mapExtractedToRecipe returns visibility private + importJobId + grounding(nutrition inferred) + confidenceScore + reviewRequired and preserves an ambiguous (null-quantity) ingredient. Update src/modules/import/README.md (Obsidian frontmatter + wikilinks + callout) documenting the extraction→confidence→persist→ready_for_review flow and the EXT-05 guarantee.
  </action>
  <verify>
    <automated>npm run test -- src/workers/import-worker.test.ts src/modules/import/import.recipe-mapping.test.ts && npm run typecheck</automated>
  </verify>
  <done>pipeline integration tests prove ready_for_review is the only success terminal and failure → extraction_failed; noSpeechDetected → reviewRequired true; mapping test green; README updated Obsidian-style; fast suite + typecheck green.</done>
</task>

</tasks>

## Artifacts this phase produces
- `src/infra/video/pipeline.ts` — `extracting` stage wired (extract → confidence → map → persist → ready_for_review; failure → extraction_failed)
- `src/modules/import/import.recipe-mapping.ts` — `mapExtractedToRecipe`
- `src/modules/import/import.recipe-mapping.test.ts`, extended `src/workers/import-worker.test.ts`
- Updated `src/modules/import/README.md`

<threat_model>
Security enforcement: ASVS L1, block on high.

| ID | STRIDE | Surface | Severity | Decision | Mitigation |
|----|--------|---------|----------|----------|------------|
| T-05-01 | Elevation / Tampering | an extraction reaching a public/published status without review | high | mitigate | The pipeline has exactly one success terminal (`ready_for_review`) and no code path to a public status; Plan 03 owner-scoping keeps the private recipe invisible to others until Phase 5 promotion. Integration test asserts no public/published status is ever written. |
| T-05-02 | Information Disclosure | transcript / LLM payload leaking into logs | medium | mitigate | The stage does not log the transcript or full LLM request/response (CONCERNS.md discipline); only ids/status/failureReason are logged. |
| T-05-03 | Tampering (data integrity) | silent-video import producing a trusted-looking recipe | high | mitigate | noSpeechDetected propagates into extraction input and forces reviewRequired via computeConfidence (Plan 04) — such a job reaches ready_for_review but flagged, never auto-trusted. |
| T-05-04 | Availability / integrity | partial write on extraction failure leaving a dangling recipe | medium | mitigate | On throw, status→failed/extraction_failed and no recipeId is linked; persistExtractedRecipe either fully inserts or throws (no half-recipe referenced by the job). |

No high-severity threat left unmitigated.
</threat_model>
