---
phase: 02-structured-extraction-recipe-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/recipes/recipe.types.ts
  - src/modules/recipes/recipe.model.ts
  - src/modules/recipes/recipe.ingestion.ts
  - src/modules/import/import-job.types.ts
  - src/config/env.ts
  - src/infra/llm/anthropic.client.ts
  - src/modules/import/__fixtures__/clean-risotto.ts
  - src/modules/import/__fixtures__/ambiguous-sparse.ts
  - src/modules/import/__fixtures__/adversarial-injection.ts
  - src/modules/recipes/recipe.ingestion.test.ts
autonomous: true
requirements: [EXT-03, EXT-04, EXT-05]
must_haves:
  truths:
    - "Recipe schema carries source='imported', visibility ('private'|'public'), importJobId back-reference, denormalized sourceMeta, and a per-field grounding blob"
    - "persistExtractedRecipe accepts visibility/importJobId/sourceMeta/grounding via IngestOptions and defaults visibility to 'public' for existing callers (EXT-03 canonicalization path untouched)"
    - "ImportJob carries recipeId, reviewRequired, confidenceScore fields for Phase 3 consumption"
    - "An IMPORT_EXTRACTION_MODEL constant resolves from env (default Sonnet per D-15), effortOption works for whichever model is configured"
    - "Three fixture transcript+caption pairs (clean, ambiguous/sparse, adversarial/injection) exist for downstream grounding tests"
  artifacts:
    - src/modules/recipes/recipe.types.ts
    - src/modules/recipes/recipe.model.ts
    - src/modules/recipes/recipe.ingestion.ts
    - src/modules/import/import-job.types.ts
    - src/infra/llm/anthropic.client.ts
    - src/modules/import/__fixtures__/clean-risotto.ts
    - src/modules/import/__fixtures__/ambiguous-sparse.ts
    - src/modules/import/__fixtures__/adversarial-injection.ts
    - src/modules/recipes/recipe.ingestion.test.ts
  key_links:
    - "IngestOptions.visibility default 'public' preserves all existing catalog/adapt/batch ingestion callers unchanged"
    - "effortOption must key off the model string actually used for import extraction, not the module-level EXTRACTION_MODEL, when they differ"
---

<objective>
Lay the schema + configuration + test-scaffolding foundation every other Phase 2 plan builds on: extend Recipe/ImportJob types and the BSON model with the net-new fields (source 'imported', visibility, grounding, importJobId, denormalized sourceMeta, recipeId/reviewRequired/confidenceScore), extend IngestOptions + persistExtractedRecipe to thread those fields WITHOUT touching the canonicalization/embedding loops (EXT-03/EXT-04), add an env-driven import-extraction model constant (D-15 Sonnet default), and commit the fixtures + the previously-missing recipe.ingestion.test.ts baseline (Nyquist Wave 0).

Purpose: This is the shared-file base. Downstream extraction (02), owner-scoped search (03), confidence gate (04), and pipeline integration (05) all import these types; doing schema churn here once avoids merge conflicts across parallel waves.
Output: Extended recipe/import types + BSON model, extended IngestOptions/persist call, import-extraction model config, 3 fixtures, and a passing recipe.ingestion.test.ts covering canonicalization-reuse (EXT-03) and imported-embedding (EXT-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structured-extraction-recipe-persistence/02-CONTEXT.md
@.planning/phases/02-structured-extraction-recipe-persistence/02-RESEARCH.md
@.planning/phases/02-structured-extraction-recipe-persistence/02-PATTERNS.md
@.planning/phases/02-structured-extraction-recipe-persistence/02-VALIDATION.md

@src/modules/recipes/recipe.types.ts
@src/modules/recipes/recipe.model.ts
@src/modules/recipes/recipe.ingestion.ts
@src/modules/import/import-job.types.ts
@src/infra/llm/anthropic.client.ts
@src/config/env.ts
@src/workers/import-worker.test.ts
</context>

<artifacts_this_phase_produces>
This plan produces: Recipe.visibility/grounding/importJobId/sourceMeta fields, RecipeVisibility type, RecipeSource 'imported' value, IngestOptions.visibility/importJobId/sourceMeta, ImportJob.recipeId/reviewRequired/confidenceScore + ImportFailureReason 'extraction_failed', IMPORT_EXTRACTION_MODEL constant, __fixtures__ pairs, recipe.ingestion.test.ts.
Consumed downstream by: 02 (import.extraction), 03 (owner-scoped search reads visibility/createdBy), 04 (confidence reads grounding shape), 05 (pipeline persists with these opts).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Recipe + ImportJob types and BSON model with imported-recipe fields</name>
  <read_first>
    - src/modules/recipes/recipe.types.ts (RecipeSource union lines 1-7; RecipeIngredient/RecipeStep lines 34-50; Recipe interface lines 71-104)
    - src/modules/recipes/recipe.model.ts (source enum lines 98-101; required array lines 55-72; index block lines 129-136; creatorSchema/nutritionSchema pattern lines 33-51)
    - src/modules/import/import-job.types.ts (ImportJobStatus lines 8-14; ImportFailureReason lines 16-25; ImportJob interface lines 27-56)
    - .planning/phases/02-structured-extraction-recipe-persistence/02-PATTERNS.md (recipe.types.ts / recipe.model.ts / import-job.types.ts sections)
  </read_first>
  <files>src/modules/recipes/recipe.types.ts, src/modules/recipes/recipe.model.ts, src/modules/import/import-job.types.ts</files>
  <behavior>
    - RecipeSource union accepts 'imported' as a valid value (per D-13); RecipeVisibility type = 'private' | 'public' (per Open Question 2 resolution, minimal 2-value enum).
    - Recipe interface exposes: visibility (RecipeVisibility, required), grounding (optional per-field blob), importJobId (optional string), sourceMeta (optional { platform, authorHandle?, authorUrl?, sourceUrl }), reviewRequired (optional boolean), confidenceScore (optional number 0..1).
    - BSON schema for RecipeModel accepts an inserted doc with source 'imported', visibility 'private', importJobId, a nested grounding object, sourceMeta object, reviewRequired, confidenceScore — and still accepts existing catalog docs (visibility kept OPTIONAL in the BSON required array to avoid breaking existing docs; defaulted at app layer).
    - ImportJob interface exposes recipeId (optional string), reviewRequired (optional boolean), confidenceScore (optional number); ImportFailureReason accepts 'extraction_failed'.
  </behavior>
  <action>
    In recipe.types.ts: add `| "imported"` to the RecipeSource union following the existing one-value-per-line + trailing-comment style. Add a new `RecipeVisibility` string-union type ('private' | 'public') modeled on the NutritionGoal/Equipment style. Define a grounding shape type for imported recipes: a `RecipeGrounding` interface holding `titleGrounding` (a `GroundingLevel` = 'grounded' | 'inferred' | 'ambiguous'), per-ingredient `quantityGrounding` keyed by canonicalId or index, per-step `grounding`, a hardcoded `nutrition: "inferred"` marker, and `sourceDivergence: string[]` — keep it flexible/import-only. Extend the Recipe interface with `visibility: RecipeVisibility` (required, matching how `source` is required), and optional `grounding?: RecipeGrounding`, `importJobId?: string`, `sourceMeta?: { platform: string; authorHandle?: string; authorUrl?: string; sourceUrl: string }`, `reviewRequired?: boolean`, `confidenceScore?: number` — place each near the existing optional denormalized fields (avgRating/ratingCount) following the same comment style. In recipe.model.ts: add "imported" to the source enum array; add BSON property schemas for `visibility` (string enum ['private','public']), `importJobId` (bsonType 'objectId'), `reviewRequired` (bool), `confidenceScore` (number), a nested `groundingSchema` object const (mirror creatorSchema/nutritionSchema style) and a `sourceMetaSchema` object const; do NOT add `visibility` to the top-level required array (existing catalog docs lack it — default at app layer only for imported, per D-13 / PATTERNS note about mongoat validation on every insert). Add a sparse index `{ key: { importJobId: 1 }, name: "import_job_lookup", sparse: true }`. In import-job.types.ts: add `recipeId?: string`, `reviewRequired?: boolean`, `confidenceScore?: number` to the ImportJob interface with inline comments matching the existing style; add `| "extraction_failed"` to ImportFailureReason.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>typecheck passes; RecipeSource accepts 'imported'; Recipe requires visibility; ImportJob exposes recipeId/reviewRequired/confidenceScore; ImportFailureReason accepts 'extraction_failed'; BSON model source enum includes 'imported' and has visibility/importJobId/grounding/sourceMeta property schemas without visibility in the required array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend IngestOptions + persistExtractedRecipe and add IMPORT_EXTRACTION_MODEL</name>
  <read_first>
    - src/modules/recipes/recipe.ingestion.ts (IngestOptions lines 34-38; persist call site lines 82-145, esp. conditional-spread block 132-136; canonicalization loop lines 89-103; embedding lines 105-116)
    - src/infra/llm/anthropic.client.ts (EXTRACTION_MODEL line 7; effortOption lines 15-19)
    - src/config/env.ts (anthropic block lines 47-56)
    - src/workers/import-worker.test.ts (mock style for @/config/env.js lines 12-21 and model classes — mirror for the new test)
  </read_first>
  <files>src/modules/recipes/recipe.ingestion.ts, src/infra/llm/anthropic.client.ts, src/config/env.ts, src/modules/recipes/recipe.ingestion.test.ts</files>
  <behavior>
    - IngestOptions accepts optional visibility (RecipeVisibility), importJobId (string), sourceMeta ({ platform, authorHandle?, authorUrl?, sourceUrl }), grounding (RecipeGrounding).
    - persistExtractedRecipe writes visibility (defaulting to 'public' when opts.visibility is absent, preserving existing catalog/adapt/batch callers unchanged), and conditionally spreads importJobId/sourceMeta/grounding only when present — using the established `...(value && { field: value })` idiom (never explicit undefined, per Mongoat gotcha).
    - The canonicalization loop (EXT-03) and embedding call (EXT-04) are UNCHANGED — no parallel matching or embedding logic added.
    - IMPORT_EXTRACTION_MODEL resolves from a new env var (default Claude Sonnet per D-15) with fallback to the shared anthropic.model; effortOption can be evaluated against whichever model string import extraction actually uses.
    - recipe.ingestion.test.ts: a recipe ingested with opts.source='imported', visibility='private' calls resolveCanonicalForIngestion exactly once per unique ingredient name (EXT-03), calls embeddings.embedDocuments with the same buildEmbeddingText shape used for catalog recipes (EXT-04), and inserts a doc carrying source 'imported' + visibility 'private'.
  </behavior>
  <action>
    In recipe.ingestion.ts: extend IngestOptions with `visibility?: RecipeVisibility`, `importJobId?: string`, `sourceMeta?: {...}`, and `grounding?: RecipeGrounding` (import the types from recipe.types.js), following the exact optional-field style of the existing parentRecipeId/createdBy. In the RecipeModel.insert({...}) call, add `visibility: opts.visibility ?? "public"` (default public keeps every existing caller's behavior identical) plus `...(opts.importJobId && { importJobId: opts.importJobId })`, `...(opts.sourceMeta && { sourceMeta: opts.sourceMeta })`, `...(opts.grounding && { grounding: opts.grounding })` in the same conditional-spread block as parentRecipeId/createdBy. Do NOT modify the canonicalization loop (lines 89-103) or the embedding block (lines 105-116) — EXT-03/EXT-04 mandate reuse. In anthropic.client.ts: add `export const IMPORT_EXTRACTION_MODEL = env.anthropic.importModel ?? env.anthropic.model;` and parametrize effortOption to accept an optional model argument (default the module EXTRACTION_MODEL) so the import path can pass IMPORT_EXTRACTION_MODEL — the regex `/haiku|sonnet-4-5/` must run against the model actually used (Sonnet 4.5 omits effort). In env.ts anthropic block: add `importModel: optional("IMPORT_EXTRACTION_MODEL", "claude-sonnet-4-5")` (D-15: Sonnet is the locked import-extraction model; using Sonnet per user decision — do NOT hardcode opus, and the catalog default stays claude-haiku-4-5 per Pitfall 1). Create recipe.ingestion.test.ts mirroring the import-worker.test.ts mock style: mock @/config/env.js, mock resolveCanonicalForIngestion (spy asserting call count per unique name), mock embeddings.embedDocuments (assert it receives the buildEmbeddingText output), mock RecipeModel.insert (capture the inserted doc); assert source='imported' + visibility='private' + one canonicalization call per unique ingredient + one embed call.
  </action>
  <verify>
    <automated>VITEST_EXCLUDE_INTEGRATION=true npm run test -- src/modules/recipes/recipe.ingestion.test.ts</automated>
  </verify>
  <done>recipe.ingestion.test.ts passes; persistExtractedRecipe defaults visibility to 'public' for existing callers and writes 'private'/importJobId/sourceMeta/grounding when supplied; canonicalization + embedding loops unchanged; IMPORT_EXTRACTION_MODEL resolves to Sonnet by default; effortOption evaluates the import model string; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Commit fixture transcript+caption pairs for downstream grounding tests</name>
  <read_first>
    - .planning/phases/02-structured-extraction-recipe-persistence/02-RESEARCH.md (Code Examples > Reconciliation Prompt Skeleton; Pitfalls 3/4/5; Wave 0 Gaps fixtures item)
    - .planning/phases/02-structured-extraction-recipe-persistence/02-VALIDATION.md (Wave 0 Requirements fixtures line; Manual-Only Verifications table)
    - src/modules/import/import-job.types.ts (ImportJob transcript/caption/noSpeechDetected shape lines 37-45)
  </read_first>
  <files>src/modules/import/__fixtures__/clean-risotto.ts, src/modules/import/__fixtures__/ambiguous-sparse.ts, src/modules/import/__fixtures__/adversarial-injection.ts</files>
  <behavior>
    - Each fixture exports a typed object with { transcript?: string; caption?: string; noSpeechDetected: boolean; label: string } plus a short human-readable note describing the expected grounding outcome.
    - clean-risotto: a rich transcript (carnaroli rice, stock, technique) + a caption with a written ingredient list — most fields should be groundable; used for the manual truthfulness spot-check.
    - ambiguous-sparse: noSpeechDetected true (or near-empty transcript) with a hashtag-only caption ('🔥🔥🔥 #foryou #fy'-style) — must expose the Pitfall 5 hallucination-from-nothing case.
    - adversarial-injection: a caption containing an injection attempt string (e.g. an instruction telling the model to mark all fields grounded) plus a plausible cooking transcript — used to assert grounding is not blindly 'grounded' and the injection text is treated as data.
  </behavior>
  <action>
    Create the __fixtures__ directory under src/modules/import/. Author three TypeScript fixture modules, each exporting a const of shape `{ transcript?: string; caption?: string; noSpeechDetected: boolean; label: string; expected: string }` where `expected` is a short prose note (not a machine assertion) describing the intended grounding outcome for the manual spot-check. Fill clean-risotto with a realistic pt-BR risotto Short transcript (mirror the risotto example validated in Phase 1 per 01/02-CONTEXT.md) and a caption containing a written ingredient list with a mix of stated and unstated quantities. Fill ambiguous-sparse with noSpeechDetected=true, no transcript, and a hashtag/emoji-only caption to exercise Pitfall 5. Fill adversarial-injection with a normal-looking cooking transcript and a caption that embeds an instruction-style injection attempt (kept as an inert data string in the fixture; describe its role in the `expected` note) to exercise Pitfall 4. Do not import these into production code — they are test-only inputs.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Three fixture modules exist under src/modules/import/__fixtures__/, each exporting a typed transcript+caption+noSpeechDetected fixture with an expected-outcome note; typecheck passes; fixtures are not referenced from production code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema → app | New Recipe/ImportJob fields must not silently widen what an unauthenticated/other-user read can see (visibility is the new access-control axis) |
| env → worker | New IMPORT_EXTRACTION_MODEL is operator-controlled config, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Recipe.visibility field | high | mitigate | Field is added here but access enforcement is Plan 03 (owner-scoped search). This plan defaults IngestOptions.visibility to 'public' for existing callers ONLY — imported recipes are always persisted 'private' by Plan 05; no query path reads visibility yet, so no leak is introduced by this plan alone. |
| T-02-02 | Tampering | BSON required array | medium | accept | visibility intentionally kept OUT of the BSON required array so existing catalog docs (which lack it) still validate; app-layer default ('public') covers the gap. Documented, not silently required. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan (RESEARCH Package Legitimacy Audit: zero installs; @anthropic-ai/sdk + zod already present). No [ASSUMED]/[SUS] packages → no install checkpoint needed. |
</threat_model>

<verification>
- `npm run typecheck` clean.
- `VITEST_EXCLUDE_INTEGRATION=true npm run test -- src/modules/recipes/recipe.ingestion.test.ts` green.
- Grep confirms canonicalization loop (resolveCanonicalForIngestion) and embedDocuments call in recipe.ingestion.ts are byte-identical to pre-change except surrounding insert fields.
</verification>

<success_criteria>
- Recipe/ImportJob types + BSON model carry all net-new imported-recipe fields; existing catalog ingestion still validates and defaults visibility 'public'.
- IMPORT_EXTRACTION_MODEL resolves to Sonnet by default (D-15) and effortOption respects it.
- Three fixtures + baseline recipe.ingestion.test.ts committed (Nyquist Wave 0 for EXT-03/EXT-04).
</success_criteria>

<output>
Create `.planning/phases/02-structured-extraction-recipe-persistence/02-01-SUMMARY.md` when done.
</output>
