# URL do backend Fastify (usada no fetch server-side do Next)
API_BASE_URL=http://localhost:3000

# Clerk (https://dashboard.clerk.com). Sem chaves, a auth fica desabilitada
# e o app roda normalmente (sem botões de login).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
